# Uncomment and modify below to explicitly set the preferred
# implementation for an ancestor rapp specification:
preferred:
  # when there are several child implementations
  'rocon_apps/chirp': 'rocon_apps/moo_chirp'
  # when an ancestor is an implementation and you specifically want the ancestor
  'rocon_apps/talker': 'rocon_apps/talker'
